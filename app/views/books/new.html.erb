<h1>New book</h1>

<%= form_tag(url_for(:action => 'fetch_amz'), :remote => true, :id => "result_form") do %>
  <%= text_field_tag :my_isbn %>
  <%= submit_tag 'Search' %>
<% end %>

<div id="img"></div>

<%= render 'form' %>

<%= link_to 'Back', books_path %>
